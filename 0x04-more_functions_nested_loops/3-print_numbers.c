#include <stdio.h>
#include <stdlib.h>
#include "main.h"
/**
 * print_numbers - function
 * Return: smthng
 */
void print_numbers(void)
{
	int a;

	for (a = (int)'0'; a <= (int)'9'; a++)
		_putchar(a);
	_putchar('\n');
}
